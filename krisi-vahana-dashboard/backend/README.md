# Krisi Vahana Tomato AI Backend

A FastAPI-based backend for tomato detection, ripeness estimation,
damage detection, and size classification.

## ▶️ Run the backend

```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
